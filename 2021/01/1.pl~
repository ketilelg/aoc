#!/opt/local/bin/perl

@lines = <>;

print @lines;

for $i ( 0 .. $#lines ) {
   for $j ( ($i + 1) .. $#lines ){
      if ( $lines[$i] + $lines [$j] == 2020 ) { print "found it:  $lines[$i] $lines[$j]"; }
}
}


